DIGITAL PORTFOLIO V6.1.1 - CLOUDFLARE DEPLOYMENT
=================================================

Recommended GitHub cleanup:
- Delete the entire old folder: app/(en)
- Then upload/commit the V6.1.1 project files.

V6.1.1 is also defensive: during Cloudflare prebuild it automatically removes
app/(en) from the ephemeral build checkout if the stale folder still exists.

Cloudflare build settings:
Node version: 22.16.0
PNPM version: 10.11.1
Build command: pnpm run build
Build output directory: out

Do NOT set SKIP_DEPENDENCY_INSTALL=1 unless the build command explicitly installs
packages first. The screenshot confirms Cloudflare's normal install step is working.

Expected prebuild log before Next.js starts:
  Removed retired legacy route group: app/(en)   [only if stale tree exists]
  Prebuild route guard passed: 4 page files resolve to 4 unique routes.
    /
    /da
    /da/work/marzieh-nail-atelier
    /work/marzieh-nail-atelier

Then Next.js should proceed to the production build.
