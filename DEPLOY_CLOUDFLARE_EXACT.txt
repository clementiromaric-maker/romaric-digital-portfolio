Cloudflare deployment settings for this repository

IMPORTANT
1. Replace the repository contents instead of merging over an older tree.
2. In Cloudflare build variables, REMOVE SKIP_DEPENDENCY_INSTALL if it is set to 1.
3. Keep Node 22.16.0 and pnpm 10.11.1 if you already use them.

Build command:
pnpm run build

Build output directory:
out

If you intentionally keep SKIP_DEPENDENCY_INSTALL=1, then the build command must be:
pnpm install --frozen-lockfile && pnpm run build

The earlier failure was caused by old external font imports at app/layout.jsx lines 1-8. V1.8.5.4 retains the V1.8.5.2 self-contained font fix, so those imports and dependencies remain removed.
